# Outputs never reach git: nbstripout runs as a pre-commit hook (`uv run pre-commit install`)
# and, if you also run `uv run nbstripout --install`, as a git clean filter. See docs/students-and-coding-agents.md, step 4.
*.ipynb filter=nbstripout
*.ipynb diff=ipynb
